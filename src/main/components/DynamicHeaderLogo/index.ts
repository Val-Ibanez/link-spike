// Componente agnóstico (sin hooks)
export { default as DynamicHeaderLogo } from './DynamicHeaderLogo';

// Wrapper con hooks (para uso en componentes que necesiten estado)
export { default as DynamicHeaderLogoWrapper } from './DynamicHeaderLogoWrapper';

// Export por defecto del wrapper para compatibilidad
export { default } from './DynamicHeaderLogoWrapper';
